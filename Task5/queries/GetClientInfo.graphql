fragment AllClientFields on Client {
    id
    name
    age
}

query GetShortClientInfo {
    client(id: "user-123") {
        id
        name
    }
}

query GetAllFieldsClientInfo {
    client(id: "user-123") {
        ...AllClientFields
    }
}
